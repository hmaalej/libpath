libpath is a C library that computes shortest ship trajectories.
It is built on a previous release of Sertak Karaman (RRT* algorithm).  


INSTALL

  Requirements

     * libjson-c (http://)
     * libglib2 (http)
     * libgeos >= 3.2 (http)

     Under Ubuntu 12.04
 
      sudo apt-get install libjson0-dev libglib2.0-dev libgeos-dev

  Compilation

     make

  Command-line tool

    make path


USAGE

  path command

    ./path -x 81.15 -y 16.16 -a -81.67 -b 7.02 -o obstacles.geojson 


  For library usage, checkout documentation (html folder), or path_main.c as
  an example.


AUTHORS


    Houssem


LICENSE

    GPL ? LPGL ? BSD ? MIT ?
